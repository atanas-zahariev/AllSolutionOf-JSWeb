<main>
    <section id="details-page">
        <h1>Details</h1>
        <article class="details-card">

            <article class="details-card-text">
                <h2>Title:<br> {{item.title}}</h2>
                <h3>Author: {{item.owner.username}}</h3>
                <h3>Painting technique: {{item.paintingTech}}</h3>
                <h3>Certificate of authenticity: {{item.certificate}}</h3>

                <!-- If there is no registered user, do not display buttons-->
                {{#if user}}
                <div class="buttons">
                    <!-- Only for registered user and author of the publication -->
                    {{#if isOwner}}
                    <a href="/galery/edit/{{item._id}}" class="btn-edit">Edit</a>
                    <a href="/galery/delete/{{item._id}}" class="btn-delete">Delete</a>
                    {{else}}
                        <!-- logged in user who has not yet shared the publication-->
                        {{#unless isTaked}}
                        <a href="/galery/action/{{item._id}}" class="btn-share">Share publication</a>
                        {{else}}
                        <!-- logged in user who has already shared the publication-->
                        <p class="shared-pub">You already shared this publication</p>
                        {{/unless }}
                    {{/if}}
                </div>
                {{/if}}
            </article>

            <article class="details-card-image">
                <img src="{{item.imgUrl}}" alt="art-image2">
            </article>

        </article>
    </section>