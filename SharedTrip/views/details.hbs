<section class="py-5 details" id="trip-details-page">
    <div class="container">
        <div class="destination">
            <div>
                <span class="icon"><i class="fas fa-map-marked-alt"></i></span>
                <h5> from <span>{{item.startPoint}}</span> to <span>{{item.endPoint}}</span> </h5>
            </div>
            <div>
                <span class="icon"><i class="fas fa-calendar-alt"></i></span>
                <h5> <span>{{item.date}}</span> at <span>{{item.time}}</span> </h5>
            </div>
        </div>
        <p class="line"></p>
        <div class="buddies-info">
            <i class="fas fa-users buddies"></i>
            <h5>Shared trip Buddies</h5>
            <div>
                <!-- if there are joined buddies for the current trip separate them with comma and space", " -->
                {{#if buddies}}
                <p>{{buddies}}</p>

                <!-- If not display: -->
                {{else}}
                <p>there are no buddies yet...</p>
                {{/if}}
            </div>
            <h5>Driver: <span>{{item.owner.email}}</span> </h5>
        </div>
        <p class="line"></p>
        <h5 class="brand">Car Brand: <span class="lead">{{item.brand}}</span></h5>
        <div class="trip-info">
            <div>
                <img class="img-fluid rounded"
                    src="{{item.imgUrl}}"
                    alt="imgUrl">
            </div>
            <div class="trip-desc">
                <h5>Information about the trip</h5>
                <textarea class="lead"
                    disabled>{{item.description}}</textarea>
                <h5>Price: <span class="lead">40</span> BGN</h5>

                <!-- if there are no logged in user do not show div with class actions  -->
                {{#if user}}
                <div class="actions">
                    <!-- Only for logged user and creator to this trip -->
                    {{#if isOwner}}
                    <a href="/trips/delete/{{item._id}}" class="btn btn-danger">Delete this trip</a>
                    <a href="/trips/edit/{{item._id}}" class="btn btn-warning">Edit this trip</a>

                    <!-- logged in user with available seats -->
                    {{else}}
                            {{#if availabelSeats}}
                                {{#unless isInCollection}}
                                <a href="/trips/userAction/{{item._id}}" class="btn btn-join">Join now, {{item.seats}} seats left!</a>
                                <!-- logged in user and has already joined the trip  -->
                                {{else}}
                                <span class="btn btn-info">Already joined. Don't be late!</span>
                                {{/unless}}
                            <!-- logged in user with no available seats -->
                            {{else}}
                            <span class="btn btn-secondary">No seats available! </span>
                            {{/if}}
                    {{/if}}
                </div>
                {{/if}}

            </div>
        </div>
    </div>
</section>